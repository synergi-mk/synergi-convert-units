var convert = require('../lib'),
  assert = require('assert'),
  tests = {};

tests['% to ppm'] = function () {
// assert.Equal(convert(1).from('%').to('ppm'), 0.0000001);

 return true;
};

module.exports = tests;
